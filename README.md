google keep ripoff with basic CRUD!

A small project that is developed more by a learning, fun approach rather than a production one.
just install flask with `pip install flask` and run `python3 memnotes/api.py`, visit the link shown by the terminal at port 5000, and enjoy.
